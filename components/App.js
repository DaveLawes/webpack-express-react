import React, { Component } from 'react'
import { render } from 'react-dom'

class App extends Component {
  render() {
    return(
      <h1>This view is being rendered by React and served by Express!</h1>
    )
  }
}

export default App;
